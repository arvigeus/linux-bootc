# Module resolution order for each entry:
#   1. modules/<path>/${DISTRO}.sh        (per-distro directory)
#   2. modules/<path>.${DISTRO}.sh        (per-distro suffix)
#   3. modules/<path>.sh                  (shared)
# Use "<path>/" to source all scripts in a directory.

# Must be first
base/init
base/config-parsers

base/distro
base/flatpak
base/appimage
base/sudo
hardware/firmware
dev/editors/
dev/tools/
entertainment/codecs
entertainment/video/
